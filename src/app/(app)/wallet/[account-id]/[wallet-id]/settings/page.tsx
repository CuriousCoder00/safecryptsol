import Settings from "@/components/wallet-settings/settings";
import React from "react";

const WalletSettings = () => {
  return (
    <div className="flex w-full h-full px-2 overflow-hidden">
      <Settings />
    </div>
  );
};

export default WalletSettings;
